# Протокол BMS ↔ JDY-18 (BLE)

Реконструйовано декомпіляцією штатних Android-додатків (`app/64db35e7d839d.apk` та
`app/666815497d87b.apk`, обидва зібрані на фреймворку E4A). Протокол в обох
APK ідентичний — відрізняється лише обфускація коду.

## З'єднання

Незважаючи на те, що JDY-18 зазвичай позиціонують як "прозорий serial-to-Bluetooth",
додаток підключається через **BLE (GATT)**, а не Classic SPP:

- Service UUID: `0000FFE0-0000-1000-8000-00805F9B34FB`
- Characteristic UUID (і запис, і notify — та сама характеристика): `0000FFE1-0000-1000-8000-00805F9B34FB`

Після підключення й увімкнення notify (запис CCCD `0x2902` = `0x0100`) BMS сам
постійно надсилає серію 19-байтних пакетів — команда "дай дані" не потрібна.

## Формат вхідних пакетів (BMS → телефон, notify на FFE1)

19 байт. `byte[0]` — тип "сторінки". Немає заголовка/маркера кадру і немає
контрольної суми — межа кадру = межа BLE-нотифікації. Усі багатобайтові поля —
**big-endian**.

### Page 0x01 (byte[0] == 1) — суміш уставок захисту й живої телеметрії

**Підтверджено власником пристрою:** offset 1-12 — це НЕ живі показники батареї,
а "відлуння" уставок захисту (кожні 2 байти — readout відповідної команди
запису 0x01-0x06, offset зростає в тому ж порядку, що й коди команд, див.
таблицю команд нижче). offset 13-18 — навпаки, **підтверджена жива телеметрія
батареї** (напруга/струм — на реальному пристрої, потужність — за тим самим
шаблоном, не перевірено окремо):

| offset | формула | поле | readout команди | впевненість |
|---|---|---|---|---|
| 1-2 | /100.0 → V | Напруга відсічки розряду (В/комірку) | 0x01 | підтверджено |
| 3-4 | /10.0 → A | Номінальний струм реле/MOSFET захисту (НЕ живий струм навантаження) | 0x02 | підтверджено |
| 5-6 | /10.0 → Ah | Максимальна (номінальна) ємність батареї | 0x03 | підтверджено |
| 7-8 | int → "S" | Налаштована кількість послідовних комірок | 0x04 | підтверджено |
| 9-10 | /100.0 → V | Напруга відсічки заряду (В/комірку) | 0x05 | підтверджено |
| 11-12 | /10.0 → °C | Уставка макс. температури (ймовірно балансувальних модулів, не підтверджено) | 0x06 | підтверджено |
| 13-14 | /10.0 → V | Жива напруга батареї | — | підтверджено |
| 15-16 | /10.0 → A | Живий струм батареї | — | підтверджено |
| 17-18 | /10.0 → кВт | Жива потужність батареї | — | підтверджено |

Струм (тут і в offset 15-16) — завжди беззнакова величина: датчик струму не
надає інформації про напрям заряд/розряд (підтверджено власником пристрою).

Застосунок показує живу напругу/струм/потужність батареї (offset 13-18) на
дашборді першою групою, а мін/макс напругу окремих комірок — окремо
(сторінка 0x11).

### Page 0x08 (byte[0] == 8)

| offset | формула | поле |
|---|---|---|
| 15-16 | /10.0 → Ah | Використана ємність батареї (ВИПРАВЛЕНО: раніше помилково читали offset 13-14, яке на реальному пристрої завжди `00 00` — саме тому показник стабільно був 0,00 Аг; перевірено побайтово на 416 кадрах у `logs/bms_20260918_124228.txt`) |

### Page 0x1F=31 (byte[0] == 31) — параметри/налаштування

| offset | формула | поле |
|---|---|---|
| 1-2 | int → сек | Затримка передзаряду |
| 3-4 | /100.0 → V | Поріг захисту по різниці напруг комірок |
| 5-6 | 1=ON / 2=OFF | Автоскидання накопиченої ємності |
| 7-8 | int | Поріг захисту від низької температури |
| 9-10 | int | Тип датчика струму (холл-сенсор), захищено паролем "770921" |
| 11-12 | int → °C | Температура старту вентилятора |
| 13-14 | int → °C | Температура старту нагрівача |
| 15-16 | int | CAN send ID, захищено паролем "770921" |
| 17-18 | int | CAN receive ID, захищено паролем "770921" |

Поля 11-18 ідентифіковано зіставленням з командами запису 0x1A-0x1D у `C1.java` (кнопки 24-27) — впевненість висока (збіг меж валідації й позиції в UI), але не HCI-підтверджено.

### Page 0x18=24 (byte[0] == 24)

Цей кадр несе одразу два непов'язаних набори даних — різні вікна штатного
застосунку читають різні байти того самого пакета:

| offset | формула | поле |
|---|---|---|
| 1-2, 3-4, 5-6 | /1000.0 → V | Напруги комірок 46, 47, 48 |
| 7-8, 9-10, 11-12 | /1000.0 → V | Напруги комірок 94, 95, 96 |
| 13-16 | не використовується | — |
| 17-18 | int, 0 = немає | Номер комірки, що спричинила спрацювання захисту |

(6 "залишкових" комірок, які не влізли в жодну з регулярних 9-коміркових сторінок 19-29 нижче.)

### Page 0x10=16 (byte[0] == 16) — стани/захист

**Уточнення термінології (власник пристрою):** те, що штатний застосунок називає
"канал" (通道) — насправді не окремі канали заряду/розряду, а ввімкнена/вимкнена
батарея (силове коло) загалом. Команда 0x07 не обирає "заряд чи розряд" — вона
просто вмикає/вимикає батарею; якщо напруга в допустимому діапазоні, BMS сама
вмикає потрібні реле. При заряді спершу вмикається пускове (стартове) реле,
і лише після витримки часу — основне реле. Живі стани реле заряду/розряду
підтверджено окремо, знайдено в `C2.java` (не в `C1.java`, звідки взято решту
цієї сторінки) — поля `充电mos` (Charge MOSFET) і `放电mos` (Discharge MOSFET):

| offset | формула | поле |
|---|---|---|
| 1-2 | /100.0 → V | Напруга відновлення заряду |
| 3-4 | /100.0 → V | Напруга відновлення розряду |
| 5-6 | 1=ON / 2=OFF | Налаштований стан батареї за замовчуванням (readout команди 0x0C) — НЕ живий стан реле |
| 7-8 | enum 0-10 | Код захисту (таблиця нижче) |
| 9-10, біт 0 | 1=ON | Живий стан реле розряду (放电mos) |
| 9-10, біт 1 | 1=ON | Живий стан реле заряду (充电mos) |
| 11-12 | 0=екран увімк., 1=вимк. | Стан екрана |
| 15-16 | /10.0 → °C | Температура MOSFET (магнітуда) |
| 17, біт 0 | 1=від'ємне | Знак температури MOSFET |
| 18 (останній байт) | бітове поле | Біти статусу (таблиця нижче) |

Офсет 13-14 (`/10.0`, поле "剩余"/"Remaining" у C2.java) — не ідентифіковано
остаточно (можливо залишкова ємність чи час), не реалізовано в Kotlin-порту.

Коди захисту (offset 7-8):

| код | значення |
|---|---|
| 0 | немає |
| 1 | 过电流保护 — перевищення струму |
| 2 | 过放电保护 — перерозряд |
| 3 | 过充电保护 — перезаряд |
| 4 | 过温度保护 — перегрів |
| 5 | 电池节数错误保护 — невірна кількість комірок |
| 6 | 充电MOS管故障 — несправність MOSFET заряду |
| 7 | 放电MOS管故障 — несправність MOSFET розряду |
| 8 | 低压主机断电保护 — просідання живлення хоста |
| 9 | 电池压差保护 — захист по різниці напруг комірок |
| 10 | 电池低温保护 — захист від низької температури |

Біти статусу (останній байт, offset 18):

| біт | маска | значення |
|---|---|---|
| 7 | 0x80 | "通道打开"/"通道关闭" (коло увімкнено/вимкнено) — точний фізичний сенс (чи саме пускове реле) не підтверджено |
| 6 | 0x40 | заряджається |
| 5 | 0x20 | балансування активне |
| 4 | 0x10 | тривога: напруга нижче порогу |
| 3 | 0x08 | тривога: струм вище порогу |
| 2 | 0x04 | тривога: невірна кількість комірок |
| 1 | 0x02 | тривога: напруга вище порогу |
| 0 | 0x01 | тривога: температура вище порогу |

### Page 0x11=17 (byte[0] == 17) — ємність/цикли/мін-макс напруга комірок

| offset | формула | поле |
|---|---|---|
| 1-2 | /100.0 → V | не ідентифіковано остаточно |
| 3-4 | int | Кількість комірок (референс) |
| 5-6 | int | не ідентифіковано (можливо, номер комірки, що балансується) |
| 7-8 | /100.0 → V | Напруга старту балансування при заряді |
| 9-10 | /1000.0 → V | Мінімальна напруга комірки |
| 11-12 | /1000.0 → V | Максимальна напруга комірки |
| (11-12)−(9-10) | /1000.0 → V | Різниця макс−мін напруги комірок (обчислюється додатком) |
| 13-14 | /1000.0 → V | Опорна (базова) напруга балансування |
| 15-18 (32-біт, BE) | пряме значення → Ah | Накопичена (кумулятивна) розряджена ємність |
| — | (15-18) / (page1, offset5-6) | Кумулятивна кількість циклів |

### Page 0x12=18 (byte[0] == 18) — температури модулів (банк A)

8 датчиків температури модулів. Байт 2 — бітова маска знаку (біт7→probe1 … біт0→probe8,
1 = від'ємне значення), байти 3-18 — 8 послідовних 16-бітних величин (`/10.0 → °C`).

| offset | формула | поле |
|---|---|---|
| 2 (біти 7..0) | бітмаска | знак probe1..probe8 |
| 3-4, 5-6, ..., 17-18 | /10.0 → °C, знак з байта 2 | Температура модуля 1..8 |

**На реальному пристрої ця сторінка стабільно повертає самі нулі** — власник
пристрою підтвердив, що фізично підключено лише 4 датчики, і їхні реальні
значення видно на сторінках 0x02/0x0A нижче, а НЕ тут. BMS підтримує до 16
датчиків, з яких протокол передає дані лише для 8 (два банки по 4).

### Pages 0x02 і 0x0A=10 — температури модулів (банк B, реальні дані)

**Окремий, незалежний від сторінки 0x12 набір даних** (той самий набір полів
`模块N温度1`/"Module N Temperature" у `C2.java`, але інша нумерація сторінок і
реально інші значення на пристрої — НЕ дублікат). Сторінка 0x02 несе температури
модулів 1-4, сторінка 0x0A=10 — модулів 5-8. Формат обох ідентичний, лише
підмножина офсетів (не всі 8 послідовних пар, як на сторінці 0x12):

| offset | формула | поле (0x02: модулі 1-4; 0x0A: модулі 5-8) |
|---|---|---|
| 2, біт 7 | 1=від'ємне | знак 1-го датчика в кадрі |
| 2, біт 5 | 1=від'ємне | знак 2-го датчика в кадрі |
| 2, біт 3 | 1=від'ємне | знак 3-го датчика в кадрі |
| 2, біт 1 | 1=від'ємне | знак 4-го датчика в кадрі |
| 3-4 | /10.0 → °C | 1-й датчик у кадрі |
| 7-8 | /10.0 → °C | 2-й датчик у кадрі |
| 11-12 | /10.0 → °C | 3-й датчик у кадрі |
| 15-16 | /10.0 → °C | 4-й датчик у кадрі |

**TODO (не реалізовано, не перевірено):** BMS підтримує до 16 датчиків, а байтова
структура цих сторінок ідентична 0x12 (8 послідовних пар, offset 3-4..17-18, і
всі 8 бітів байта 2 під знак). Офсети 5-6/9-10/13-14 і біти знаку 6/4/2/0 тут
не читаються — на пристрої власника вони завжди нульові, бо реально підключені
лише 4 датчики, і саме в слоти 1,3,5,7 (через один). Можливо, 0x02 насправді
несе датчики 1-8, а 0x0A — датчики 9-16 (разом
рівно 16), а штатний застосунок сам показує лише половину. Перевірити можна
лише підключивши додаткові датчики.

### Напруги комірок — ДВІ дублюючі pageType-схеми (нумерація вікон C2 і C4)

Кожен кадр несе підмножину напруг комірок (`/1000.0 → V`), offset 1-2..17-18 →
9 послідовних 16-бітних значень. Значення `0` = комірка відсутня. Штатний
застосунок передає ці самі дані ДВІЧІ, під різними pageType, залежно від того,
з якого вікна декомпільований код (C2.java чи C4.java):

| комірки | pageType (нумерація C4) | pageType (нумерація C2) |
|---|---|---|
| 1-9 | 19 | 3 |
| 10-18 | 20 | 4 |
| 19-27 | 21 | 5 |
| 28-36 | 22 | 6 |
| 37-45 | 23 | 7 |
| 46-48, 94-96 (нерегулярна) | 24 (offset 1-12; offset 17-18 — окреме поле, див. вище) | 8 (не реалізовано, TODO) |
| 49-57 | 25 | 11 |
| 58-66 | 26 | 12 |
| 67-75 | 27 | 13 |
| 76-84 | 28 | 14 |
| 85-93 | 29 | 15 |

**Підтверджено на реальному пристрої: обидві схеми парсяться одночасно, бо
"жива" (ненульова) виявляється лише ОДНА з них залежно від пристрою/прошивки**
— на пристрої власника нумерація C4 (19-29) стабільно нульова, а нумерація C2
(3-7, 11-15) дає реальні напруги. Без цього спостереження вигляд був би такий,
ніби дані відсутні. Kotlin-парсер зливає обидві схеми в один `cellVoltages`,
причому **нуль з неробочої схеми не затирає вже відоме реальне значення** з
робочої (інакше кожен цикл опитування давав "блимання" між реальним числом і
нулем в UI, поки приходили кадри то з однієї, то з іншої схеми).

Комірки 97-192 цим протоколом **не передаються** у жодній зі схем — обидві
покривають рівно комірки 1-96. Є лише прапорці активності балансування для
комірок 97-192 на окремій сторінці (30 у нумерації C4, 9 у нумерації C2) — без
напруги, не реалізовано в Kotlin-порту (точний біт→комірка мапінг не
підтверджено).

**Гіпотеза (не перевірено):** інструкція до BMS заявляє підтримку до 192 комірок
послідовно, а власний LCD-екран пристрою (не телефонний застосунок) показує
комірки групами по 48 з літерою групи (A/B/C/D) у кутку. Можливо, сторінки
19-29 (і аналогічні в C2) насправді відносяться до "поточної групи" на пристрої,
а не завжди до комірок 1-96 — і команду "перемкнути групу" в BLE-протоколі
просто не знайдено статичним аналізом декомпільованого коду (жодного китайського
терміну "组"/"分组" в жодному з класів com/xue18 немає, окремого екрана перемикання
теж немає). Кандидати на приховану команду зміни групи — 0x09, 0x12, 0x13 (їхня
точна мета й так не підтверджена, див. нижче) — варто перевірити, чи змінюються
цифри на сторінках 19-29 після натискання однієї з цих кнопок, і чи змінюються
вони при ручному перемиканні групи на екрані самого BMS (без жодної команди з
телефону — це підтвердило б, що дисплей пристрою й BLE-потік показують той самий
"поточний банк", яким телефон поки не може керувати).

## Формат вихідних команд (телефон → BMS, запис у FFE1)

5 байт:

```
byte[0]    = код команди
byte[1..2] = параметр, big-endian uint16 (для 1-байтних команд byte[1] = 0x00, byte[2] = параметр)
byte[3..4] = контрольна сума, big-endian uint16 = 0xFFFF - параметр
```

Контрольна сума — просте доповнення параметра до 0xFFFF, **не** CRC16/Modbus і
не сума байтів кадру. Команди пишуться як HEX-ASCII рядок, що конвертується у
байти (не потрібно окремо кодувати — байти йдуть напряму).

**Підтверджено на реальному пристрої** (1-байтний параметр): `0d0002fffd`
(екран OFF), `0d0001fffe` (екран ON), `10 00 02 ff fd` (вхід у режим реального
часу — обов'язково, без цього BMS не починає штовхати нотифікації з даними),
`10 00 01 ff fe` (вихід), `070001fffe`/`070002fffd` (канал), `080001fffe`/
`080002fffd` (автобалансування).

**Підтверджено на реальному пристрої** (2-байтний параметр — формула checksum
працює): 0x01 (напруга відсічки розряду), 0x02 (номінальний струм реле), 0x03
(максимальна ємність), 0x05 (напруга відсічки заряду), 0x06 (захист від
перегріву) — власник пристрою підтвердив успішну зміну всіх п'яти через UI.

**Решта команд з 2-байтним параметром ще НЕ підтверджені побайтово** (та сама
гіпотеза 0xFFFF-параметр, екстрапольована з підтвердженого випадку): 0x04,
0x0A-0x1D. Kotlin-порт реалізує їх з цією гіпотезою, з попередженням в UI на
кожне таке поле. **Перед тим як покладатися на ці команди поза UI з
попередженням, рекомендується зняти Bluetooth HCI snoop log** (Android
Developer Options → Enable Bluetooth HCI snoop log) під час зміни відповідного
налаштування у штатному додатку, щоб підтвердити побайтово реальний трафік.

Повна таблиця команд запису (код → призначення, межі, readout-сторінка):

✅ = підтверджено на реальному пристрої (2-байтний параметр, а не лише статичний аналіз).

| Код | Призначення | Межі | Readout |
|---|---|---|---|
| 0x01 ✅ | Напруга відсічки розряду | 0.01–5.00 В/комірку | сторінка 1, offset1-2 |
| 0x02 ✅ | Номінальний струм реле | 0–999.9 А | сторінка 1, offset3-4 |
| 0x03 ✅ | Максимальна ємність батареї | 0–6500.0 Аг | сторінка 1, offset5-6 |
| 0x04 | Кількість комірок | 0–192 | сторінка 1, offset7-8 |
| 0x05 ✅ | Напруга відсічки заряду | 0.01–5.00 В/комірку | сторінка 1, offset9-10 |
| 0x06 ✅ | Захист від перегріву (уставка макс. температури) | 0–150.0 °C | сторінка 1, offset11-12 |
| 0x07 | Увімкнути/вимкнути батарею (НЕ окремо заряд/розряд — BMS сама вмикає потрібні реле) | 1=off/2=on | сторінка 16, offset18 біт7 |
| 0x08 | Автобалансування увімк./вимк. | 1=off/2=on | — |
| 0x09 | "Скинути" (ціль непідтверджена) | fixed 1 | — |
| 0x0A | Напруга відновлення заряду | 0.01–5.00 В | сторінка 16, offset1-2 |
| 0x0B | Напруга відновлення розряду | 0.01–5.00 В | сторінка 16, offset3-4 |
| 0x0C | Канал за замовчуванням (1=on/2=off) | enum 1/2 | сторінка 16, offset5-6 |
| 0x0D | Екран увімк./вимк. (1=on/2=off) | enum 1/2 | сторінка 16, offset11-12 |
| 0x0E | Низька напруга відключення хоста | 0.01–5.00 В | сторінка 17, offset1-2 |
| 0x0F | Затримка відключення хоста | 1–60000 с | сторінка 17, offset5-6 |
| 0x10 | Вхід/вихід з режиму реального часу (2=enter/1=exit) | — | — |
| 0x11 | Напруга старту балансування | 0.01–5.00 В | сторінка 17, offset7-8 |
| 0x12 | Скинути розряджену ємність (ціль перетинається з 0x09/0x13) | fixed 1 | — |
| 0x13 | CLR — скинути цикли | fixed 1 | — |
| 0x14 | Використана ємність (без масштабу!) | 0–6500 | сторінка 8, offset15-16 |
| 0x15 | Автоскидання ємності (1=on/2=off) | enum 1/2 | сторінка 31, offset5-6 |
| 0x16 | Затримка передзаряду | 0–6500 с | сторінка 31, offset1-2 |
| 0x17 | Поріг різниці напруг комірок | 0–5.00 В | сторінка 31, offset3-4 |
| 0x18 | Захист від низької температури | 0–100 °C (від'ємні) | сторінка 31, offset7-8 |
| 0x19 | Тип датчика струму — 🔒 пароль "770921" | 1–3 | сторінка 31, offset9-10 |
| 0x1A | Температура старту вентилятора | 0–100 °C | сторінка 31, offset11-12 |
| 0x1B | Температура старту нагрівача | 0–100 °C | сторінка 31, offset13-14 |
| 0x1C | CAN send ID — 🔒 пароль "770921" | 1–32000, ≠0x1D | сторінка 31, offset15-16 |
| 0x1D | CAN receive ID — 🔒 пароль "770921" | 1–32000, ≠0x1C | сторінка 31, offset17-18 |

Увага: конвенція on/off непослідовна — 0x0D використовує 1=on/2=off, а
0x07/0x08/0x0C/0x15 використовують 2=on/1=off. Не уніфікувати помилково.

Команди, захищені паролем `770921` у штатному застосунку (0x19, 0x1C, 0x1D) —
заводські/розширені налаштування. Kotlin-порт не повторює цей пароль (особистий
застосунок для власного пристрою) — поля лише позначені в UI як "інженерні".

## Джерело

- Задекомпільовано інструментом jadx 1.5.1.
- Логіка застосунку (не runtime-фреймворк E4A) — у пакеті `com/xue18/`,
  файли `C1.java`, `C2.java`, `C4.java`, `C0054.java`/`C0059.java`.
- Ключові посилання: обробник вхідних пакетів — `C1.java:4088-4427`
  (метод callback'у `onCharacteristicChanged`); побудова вихідних команд —
  `C1.java:4028-5054`; hex→bytes конвертер —
  `com/e4a/runtime/C0051.java:386` (`m806`).

## Невизначене / що перевірити вручну

1. Від'ємна температура на page 1 offset 11-12 — парситься без sign-extend, від'ємні
   °C під питанням (на відміну від page 18, де знак підтверджено окремою бітовою
   маскою).
2. Page 0x11 offset 1-2 (перейменовано, помірна впевненість) та 5-6 — без
   явного текстового підпису в UI штатного застосунку.
3. Page 0x01 offset 17-18 (потужність) — та сама формула, що й для offset
   13-16 (напруга/струм, вже підтверджені на пристрої), але саму потужність
   окремо не звіряли.
4. Формула checksum для запису 2-байтних параметрів — **підтверджена на
   пристрої для команд 0x01, 0x02, 0x03, 0x05, 0x06** (успішна зміна через UI).
   Решта команд (0x04, 0x0A-0x1D) — формула та сама, але кожну окрему команду
   ще не перевірено; рекомендується зняти HCI snoop або перевірити на пристрої
   перед довірою до критичних порогів захисту, яких ще нема в списку вище.
5. Команди 0x09/0x12/0x13 (кнопки скидання) — точна ціль кожної не
   підтверджена, можливо дублюють одна одну.
6. Сторінка 30 (прапорці активності балансування для комірок 97-192) і живі
   напруги комірок 97-192 — не реалізовано, протокол їх взагалі не передає
   в тому вигляді, в якому застосунок його читає (штатний застосунок теж не
   показує).
7. `C0059.java` (друга декомпільована APK, apk2) детально не перевірявся —
   ідентичний протокол в apk1/apk2 підтверджено раніше, але не звірявся байт-в-байт.
8. **Живі напруги комірок 97-192 (192S) — досі не знайдено в жодній версії
   застосунку.** Порівняння з попередньою версією `com.xue17` (96S, v9.7, той
   самий реліз 2024-06-10) показало: різниця 96S→192S — це підняття стелі
   `state.cellCount` з 96 до 192 (команда 0x04) і абсолютно нові налаштування
   CAN-шини (0x1C/0x1D) та типу датчика струму (0x19, усі три — під паролем
   "770921"), плюс нове вікно C4 (якого в 96S нема взагалі). Але C4 **не**
   розширює покриття комірок — воно парсить ті самі сторінки 19-29/24, що й
   дають комірки 1-96 у вікні C2 (перевірено по обох версіях: `m448`-діапазон
   сторінок у C2.java ідентичний, 1-16, в обох `com.xue17` і `com.xue18`).
   Отже теорія про групи/банки по 48 комірок (LCD пристрою показує літеру
   A/B/C/D) не підтверджена на рівні коду жодної з версій — ні окремого
   екрана перемикання, ні згадки "组"/"分组" немає ніде в `com/xue17` чи
   `com/xue18`. Лишається відкритим: можливо, LCD пристрою читає внутрішню
   шину контролера напряму, а не цей BLE-канал. Перевірити на реальному
   пристрої: чи змінюються комірки 1-96 в застосунку при ручному перемиканні
   групи на LCD пристрою (без жодної команди з телефону) — якщо так, це
   BLE-канал таки показує "поточний банк", яким телефон поки не керує.
9. Page 0x10 offset 18 біт7 ("通道打开"/"通道关闭") — фізичний сенс не підтверджено
   остаточно: чи це саме пускове (стартове) реле, описане власником пристрою
   (заряд спершу через пускове реле, потім, з витримкою часу, основне), чи
   узагальнений прапорець "силове коло увімкнено" (включно з обома MOS).
   Окремих даних про власне пускове реле в декомпільованому коді не знайдено.
10. Page 0x10 offset 13-14 ("剩余"/"Remaining" у C2.java, `/10.0`) — не
    ідентифіковано остаточно (залишкова ємність? залишковий час?), не
    реалізовано в Kotlin-порту.
